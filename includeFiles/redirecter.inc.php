<?php

/*
session_start();

$isAdmin = $_SESSION['isAdmin'];
$isCoach = $_SESSION['isCoach'];
$isValid = $_SESSION['isValid'];

if (isset($_SESSION['userName'])) {
    //-----
} else {
    header('login.php');
}
 */